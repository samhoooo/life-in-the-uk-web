import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  body: {
    textAlign: "center",
  },
  quizImage: {
    width: "100%",
    maxWidth: "400px",
    height: "auto",
  },
  start: {
    marginTop: "3em",
  },
  startButton: {
    fontWeight: "bold",
  },
});

export default useStyles;
