import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  body: {
    textAlign: "center",
  },
  card: {
    margin: "auto",
    maxWidth: 500,
    padding: 20,
  },
});

export default useStyles;
